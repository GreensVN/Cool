-- GreensVN Hub ULTIMATE V7.0 - Merged & Fixed
-- By Oxbl149 & GreensVN (merged/fixed by assistant)
-- Note: This script uses many exploit-only functions (gethui, hookfunction, setreadonly, getrawmetatable, getconnections, getgc, setclipboard, writefile, etc).
-- All calls are wrapped in pcall where appropriate to avoid runtime errors in restricted environments.

-- ==== SERVICES & INIT ====
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local MarketplaceService = game:GetService("MarketplaceService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local VirtualUser = game:GetService("VirtualUser")
local player = Players.LocalPlayer

local function getGuiParent()
    local ok, res = pcall(function()
        if gethui then return gethui() end
        return CoreGui
    end)
    return ok and res or CoreGui
end

-- Helper safe http get (returns nil on failure)
local function safeHttpGet(url)
    local ok, res = pcall(function() return game:HttpGet(url) end)
    if ok then return res end
    return nil
end

-- ==== WELCOME POPUP ====
do
    local preGui = Instance.new("ScreenGui")
    preGui.Name = "GreensVNHubWelcome"
    preGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    preGui.IgnoreGuiInset = true
    preGui.ResetOnSpawn = false
    preGui.Parent = getGuiParent()

    local main = Instance.new("Frame", preGui)
    main.Name = "Main"
    main.AnchorPoint = Vector2.new(0.5, 0.5)
    main.Position = UDim2.new(0.5, 0, 0.5, -20)
    main.Size = UDim2.new(0, 465, 0, 140)
    main.BackgroundColor3 = Color3.fromRGB(255, 226, 110)
    main.ClipsDescendants = true

    local stroke = Instance.new("UIStroke", main)
    stroke.Color = Color3.fromRGB(255,255,255)
    stroke.Thickness = 4

    local shadow = Instance.new("ImageLabel", main)
    shadow.Name = "Shadow"
    shadow.BackgroundTransparency = 1
    shadow.Image = "rbxassetid://1316045217"
    shadow.ImageTransparency = 0.82
    shadow.Size = UDim2.new(1, 32, 1, 32)
    shadow.Position = UDim2.new(0, -16, 0, -16)
    shadow.ZIndex = 0

    Instance.new("UICorner", main).CornerRadius = UDim.new(0.19, 0)

    local icon = Instance.new("ImageLabel", main)
    icon.BackgroundTransparency = 1
    icon.Size = UDim2.new(0, 56, 0, 56)
    icon.Position = UDim2.new(0, 25, 0, 17)
    icon.Image = "rbxassetid://7733960981"
    icon.ZIndex = 2

    local label1 = Instance.new("TextLabel", main)
    label1.BackgroundTransparency = 1
    label1.Size = UDim2.new(1, -110, 0, 48)
    label1.Position = UDim2.new(0, 94, 0, 18)
    label1.Font = Enum.Font.GothamBlack
    label1.TextSize = 32
    label1.TextColor3 = Color3.fromRGB(64, 44, 0)
    label1.Text = "Welcome, " .. tostring(player.DisplayName or player.Name or "User")
    label1.TextXAlignment = Enum.TextXAlignment.Left
    local label1Stroke = Instance.new("UIStroke", label1)
    label1Stroke.Color = Color3.fromRGB(255,255,255)
    label1Stroke.Thickness = 2

    local label2 = Instance.new("TextLabel", main)
    label2.BackgroundTransparency = 1
    label2.Size = UDim2.new(1, -60, 0, 38)
    label2.Position = UDim2.new(0, 60, 0, 78)
    label2.Font = Enum.Font.Gotham
    label2.TextSize = 22
    label2.TextColor3 = Color3.fromRGB(100, 70, 0)
    label2.Text = "Cảm ơn vì bạn đã tin tưởng và sử dụng"
    label2.TextXAlignment = Enum.TextXAlignment.Left
    local label2Stroke = Instance.new("UIStroke", label2)
    label2Stroke.Color = Color3.fromRGB(255,255,255)
    label2Stroke.Thickness = 1.2

    -- Fade in/out safely
    main.BackgroundTransparency = 1
    icon.ImageTransparency = 1
    label1.TextTransparency = 1
    label2.TextTransparency = 1
    label1Stroke.Transparency = 1
    label2Stroke.Transparency = 1
    task.spawn(function()
        for i = 0, 1, 0.08 do
            main.BackgroundTransparency = 1 - i
            icon.ImageTransparency = 1 - i
            label1.TextTransparency = 1 - i
            label2.TextTransparency = 1 - i
            label1Stroke.Transparency = 1 - i
            label2Stroke.Transparency = 1 - i
            task.wait(0.02)
        end
        task.wait(2.5)
        for i = 0, 1, 0.1 do
            main.BackgroundTransparency = i
            icon.ImageTransparency = i
            label1.TextTransparency = i
            label2.TextTransparency = i
            label1Stroke.Transparency = i
            label2Stroke.Transparency = i
            task.wait(0.016)
        end
        preGui:Destroy()
    end)
end

-- ==== STEALTH & ADMIN DETECT ====
local stealthMode = false
local adminDetectMode = false
local STEALTH_CONNECTIONS, ADMIN_CONNECTIONS = {}, {}
local ADMINS_DETECTED = false
local mainScreenGui -- will be assigned later to the main screen GUI root

local function activateStealth()
    stealthMode = true
    pcall(function()
        for _,v in pairs({"syn","is_synapse_function","isexecutorclosure","iskrnlclosure","pebc_execute","pebc","fluxus","get_hidden_gui","gethui","get_hidden_ui","getgenv","getrenv","getgc", "getreg","getrawmetatable"}) do
            if type(getgenv) == "function" and getgenv()[v] then
                getgenv()[v] = nil
            elseif _G[v] ~= nil then
                _G[v] = nil
            end
        end
    end)

    pcall(function()
        for _,c in ipairs(STEALTH_CONNECTIONS) do pcall(function() c:Disconnect() end) end
        STEALTH_CONNECTIONS = {}
        table.insert(STEALTH_CONNECTIONS, UserInputService.WindowFocusReleased:Connect(function()
            for _,obj in pairs(getGuiParent():GetChildren()) do
                if obj:IsA("ScreenGui") and obj.Name:lower():find("greensvn") then
                    pcall(function() obj.Enabled = false end)
                end
            end
        end))
        table.insert(STEALTH_CONNECTIONS, UserInputService.WindowFocused:Connect(function()
            for _,obj in pairs(getGuiParent():GetChildren()) do
                if obj:IsA("ScreenGui") and obj.Name:lower():find("greensvn") then
                    pcall(function() obj.Enabled = true end)
                end
            end
        end))
    end)

    pcall(function()
        if _G._Stealth_KickChecker then return end
        _G._Stealth_KickChecker = true
        task.spawn(function()
            while stealthMode do
                task.wait(10)
                pcall(function()
                    if player and player.Kick and type(player.Kick) == "function" then
                        if pcall(function() return tostring(player.Kick) end) then
                            -- attempt to hook; not guaranteed in all environments
                            if hookfunction then
                                pcall(function() hookfunction(player.Kick, function() return end) end)
                            end
                        end
                    end
                end)
            end
        end)
    end)
end

local function deactivateStealth()
    stealthMode = false
    for _,c in ipairs(STEALTH_CONNECTIONS) do pcall(function() c:Disconnect() end) end
    STEALTH_CONNECTIONS = {}
    -- Re-enable GUIs
    pcall(function()
        for _,obj in pairs(getGuiParent():GetChildren()) do
            if obj:IsA("ScreenGui") and obj.Name:lower():find("greensvn") then
                pcall(function() obj.Enabled = true end)
            end
        end
    end)
end

local function isPossibleAdmin(p)
    if not p then return false end
    local ok, name = pcall(function() return (p.Name or ""):lower() end)
    local ok2, display = pcall(function() return (p.DisplayName or ""):lower() end)
    name = ok and name or ""
    display = ok2 and display or ""
    if name:find("admin") or name:find("mod") or name:find("dev") or name:find("staff") or display:find("admin") or display:find("mod") or display:find("dev") or display:find("staff") then
        return true, "Tên có từ khóa Admin/Mod/Dev/Staff"
    end
    local ok3, uid = pcall(function() return p.UserId end)
    if ok3 and type(uid) == "number" and uid < 1000000 then return true, "UserId nhỏ (Dev gốc)" end

    pcall(function()
        if p.GetFriendsOnline then
            local friends = p:GetFriendsOnline()
            if typeof(friends) == "table" then
                for _,f in ipairs(friends) do
                    if f.IsOnline and (f.Name:lower():find("roblox") or f.Name:lower():find("admin")) then
                        error("HAS_FRIEND_MATCH") -- we'll capture below
                    end
                end
            end
        end
    end)
    -- The above used error to early-return in pcall; handle gracefully
    local groups = {1200769, 2868472, 5708463, 1}
    for _, gid in ipairs(groups) do
        local okg, rank = pcall(function() return p:GetRankInGroup(gid) end)
        if okg and rank and rank >= 200 then return true, "Rank nhóm cao" end
    end
    local okIn, inGroup = pcall(function() return p:IsInGroup(1200769) end)
    if okIn and inGroup then
        local okRank, r = pcall(function() return p:GetRankInGroup(1200769) end)
        if okRank and r and r >= 200 then return true, "Roblox Staff" end
    end
    return false
end

local function showAdminPopup(msg)
    local alertGui = Instance.new("ScreenGui")
    alertGui.Name = "GreensVN_AdminAlert"
    alertGui.IgnoreGuiInset = true
    alertGui.ResetOnSpawn = false
    alertGui.Parent = getGuiParent()
    local frame = Instance.new("Frame", alertGui)
    frame.AnchorPoint = Vector2.new(0.5, 0)
    frame.Position = UDim2.new(0.5, 0, 0.04, 0)
    frame.Size = UDim2.new(0, 420, 0, 60)
    frame.BackgroundColor3 = Color3.fromRGB(230, 60, 60)
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0.23, 0)
    local label = Instance.new("TextLabel", frame)
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Font = Enum.Font.GothamBold
    label.TextSize = 20
    label.TextColor3 = Color3.new(1,1,1)
    label.Text = msg
    label.TextWrapped = true
    task.delay(5, function() pcall(function() alertGui:Destroy() end) end)
end

local function activateAdminDetect()
    adminDetectMode = true
    for _,c in ipairs(ADMIN_CONNECTIONS) do pcall(function() c:Disconnect() end) end
    ADMIN_CONNECTIONS = {}
    -- initial scan
    for _,p in pairs(Players:GetPlayers()) do
        if p ~= player then
            local ok, reason = pcall(function() return isPossibleAdmin(p) end)
            if ok and reason and reason ~= false then
                -- isPossibleAdmin may return (true, "reason") or (false)
                local isAdm, r = isPossibleAdmin(p)
                if isAdm then
                    ADMINS_DETECTED = true
                    if mainScreenGui then pcall(function() mainScreenGui.Enabled = false end) end
                    showAdminPopup("Phát hiện Admin ("..tostring(p.Name)..")\nLý do: "..tostring(r))
                end
            end
        end
    end
    table.insert(ADMIN_CONNECTIONS, Players.PlayerAdded:Connect(function(p)
        if p ~= player then
            local isAdm, r = isPossibleAdmin(p)
            if isAdm then
                ADMINS_DETECTED = true
                if mainScreenGui then pcall(function() mainScreenGui.Enabled = false end) end
                showAdminPopup("Admin vừa vào ("..tostring(p.Name)..")\nLý do: "..tostring(r))
            end
        end
    end))
    table.insert(ADMIN_CONNECTIONS, Players.PlayerRemoving:Connect(function(p)
        if p ~= player then
            local isAdm, _ = isPossibleAdmin(p)
            if isAdm then
                task.wait(1)
                local anyAdmin = false
                for _,pl in pairs(Players:GetPlayers()) do
                    if pl ~= player then
                        local isA, _ = isPossibleAdmin(pl)
                        if isA then anyAdmin = true break end
                    end
                end
                if not anyAdmin then
                    ADMINS_DETECTED = false
                    if mainScreenGui then pcall(function() mainScreenGui.Enabled = true end) end
                    showAdminPopup("Admin đã rời game! Hub đã hiện lại")
                end
            end
        end
    end))
end

local function deactivateAdminDetect()
    adminDetectMode = false
    for _,c in ipairs(ADMIN_CONNECTIONS) do pcall(function() c:Disconnect() end) end
    ADMIN_CONNECTIONS = {}
    ADMINS_DETECTED = false
    if mainScreenGui then pcall(function() mainScreenGui.Enabled = true end) end
end

-- ==== MAIN GUI SETUP ====
local MIN_SIZE = Vector2.new(350, 250)
local MAX_SIZE = Vector2.new(750, 650)
local TAB_WIDTH = 132
local AVATAR_ID = "rbxassetid://86684458914012"
local DEFAULT_SCALE = 1
local MIN_SCALE = 0.7
local MAX_SCALE = 1.6
local DEFAULT_COLOR = Color3.fromRGB(30, 32, 40)
local currentScale = DEFAULT_SCALE
local currentColor = DEFAULT_COLOR

local function applyGuiScale(mainFrame, scale)
    mainFrame.Size = UDim2.new(0, math.floor(540 * scale), 0, math.floor(590 * scale))
    mainFrame.Position = UDim2.new(0.5, -math.floor(270 * scale), 0.5, -math.floor(295 * scale))
end
local function applyGuiColor(mainFrame, newColor)
    mainFrame.BackgroundColor3 = newColor
end

mainScreenGui = Instance.new("ScreenGui")
mainScreenGui.Name = "GreensVNHubUltimate"
mainScreenGui.Parent = getGuiParent()
mainScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
mainScreenGui.ResetOnSpawn = false
mainScreenGui.IgnoreGuiInset = true -- keep consistent

-- animated background
local animatedBg = Instance.new("Frame", mainScreenGui)
animatedBg.Size = UDim2.new(1, 0, 1, 0)
animatedBg.BackgroundTransparency = 1
animatedBg.ZIndex = -1
local gradient = Instance.new("UIGradient", animatedBg)
gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(50,180,120)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(80,60,160)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(40,80,220))
}
RunService:BindToRenderStep("GreensVNBgAnim", Enum.RenderPriority.Camera.Value, function()
    if gradient then
        gradient.Rotation = (tick() * 30) % 360
    end
end)

local mainFrame = Instance.new("Frame", mainScreenGui)
mainFrame.Size = UDim2.new(0, 0, 0, 0)
mainFrame.Position = UDim2.new(0.5, -270, 0.5, -295)
mainFrame.BackgroundColor3 = DEFAULT_COLOR
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 16)
local mainStroke = Instance.new("UIStroke", mainFrame)
mainStroke.Color = Color3.fromRGB(90, 180, 90)
mainStroke.Thickness = 2

TweenService:Create(mainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back), {
    Size = UDim2.new(0, 540, 0, 590)
}):Play()

local shadow = Instance.new("ImageLabel", mainFrame)
shadow.Name = "Shadow"
shadow.BackgroundTransparency = 1
shadow.Image = "rbxassetid://1316045217"
shadow.ImageTransparency = 0.7
shadow.Size = UDim2.new(1, 40, 1, 40)
shadow.Position = UDim2.new(0, -20, 0, -20)
shadow.ZIndex = 0

-- top bar
local topBar = Instance.new("Frame", mainFrame)
topBar.Size = UDim2.new(1, 0, 0, 64)
topBar.BackgroundColor3 = Color3.fromRGB(36, 38, 50)
Instance.new("UICorner", topBar).CornerRadius = UDim.new(0, 16)

local avatarImg = Instance.new("ImageLabel", topBar)
avatarImg.Size = UDim2.new(0, 54, 0, 54)
avatarImg.Position = UDim2.new(0, 9, 0, 5)
avatarImg.BackgroundTransparency = 1
avatarImg.Image = AVATAR_ID
Instance.new("UICorner", avatarImg).CornerRadius = UDim.new(1, 0)

local titleLabel = Instance.new("TextLabel", topBar)
titleLabel.Size = UDim2.new(1, -180, 1, 0)
titleLabel.Position = UDim2.new(0, 75, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "GreensVN Hub V7.0"
titleLabel.TextColor3 = Color3.fromRGB(120, 255, 120)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 30
titleLabel.TextXAlignment = Enum.TextXAlignment.Left

local minBtn = Instance.new("TextButton", topBar)
minBtn.Size = UDim2.new(0, 34, 0, 34)
minBtn.Position = UDim2.new(1, -84, 0, 15)
minBtn.BackgroundColor3 = Color3.fromRGB(60,80,60)
minBtn.Text = "-"
minBtn.Font = Enum.Font.GothamBold
minBtn.TextSize = 26
minBtn.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0.5, 0)

local closeBtn = Instance.new("TextButton", topBar)
closeBtn.Size = UDim2.new(0, 34, 0, 34)
closeBtn.Position = UDim2.new(1, -40, 0, 15)
closeBtn.BackgroundColor3 = Color3.fromRGB(120,60,60)
closeBtn.Text = "x"
closeBtn.TextColor3 = Color3.fromRGB(255,255,255)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 18
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0.5, 0)
closeBtn.MouseButton1Click:Connect(function()
    pcall(function() mainScreenGui:Destroy() end)
end)

local function animateBtn(btn, c1, c2)
    btn.MouseEnter:Connect(function() pcall(function() TweenService:Create(btn, TweenInfo.new(0.11), {BackgroundColor3 = c2}):Play() end) end)
    btn.MouseLeave:Connect(function() pcall(function() TweenService:Create(btn, TweenInfo.new(0.13), {BackgroundColor3 = c1}):Play() end) end)
    btn.MouseButton1Down:Connect(function() pcall(function() TweenService:Create(btn, TweenInfo.new(0.11), {BackgroundColor3 = c2:lerp(Color3.new(1,0.6,0.6), 0.2)}):Play() end) end)
    btn.MouseButton1Up:Connect(function() pcall(function() TweenService:Create(btn, TweenInfo.new(0.11), {BackgroundColor3 = c2}):Play() end) end)
end
animateBtn(closeBtn, Color3.fromRGB(120,60,60), Color3.fromRGB(180,60,60))
animateBtn(minBtn, Color3.fromRGB(60,80,60), Color3.fromRGB(90,120,80))

-- sidebar / tabs
local tabNames = {"Home", "Hack", "Executor", "Library", "Settings", "Credit"}
local tabHolder = Instance.new("Frame", mainFrame)
tabHolder.Size = UDim2.new(0, TAB_WIDTH, 1, -64)
tabHolder.Position = UDim2.new(0, 0, 0, 64)
tabHolder.BackgroundColor3 = Color3.fromRGB(36, 38, 50)
Instance.new("UICorner", tabHolder).CornerRadius = UDim.new(0, 16)

local collapseBtn = Instance.new("TextButton", tabHolder)
collapseBtn.Size = UDim2.new(0, 32, 0, 32)
collapseBtn.Position = UDim2.new(1, -38, 0, 12)
collapseBtn.BackgroundColor3 = Color3.fromRGB(30, 60, 30)
collapseBtn.Text = "≡"
collapseBtn.Font = Enum.Font.GothamBold
collapseBtn.TextSize = 18
collapseBtn.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", collapseBtn).CornerRadius = UDim.new(0.5, 0)

local tabBtns = {}
for i, name in ipairs(tabNames) do
    local btn = Instance.new("TextButton", tabHolder)
    btn.Size = UDim2.new(1, -16, 0, 46)
    btn.Position = UDim2.new(0, 8, 0, (i-1)*56 + 12)
    btn.BackgroundColor3 = (i==1) and Color3.fromRGB(64, 110, 64) or Color3.fromRGB(55, 55, 65)
    btn.Text = name
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 22
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0.4, 0)
    tabBtns[i] = btn
end

local sidebarCollapsed = false
collapseBtn.MouseButton1Click:Connect(function()
    sidebarCollapsed = not sidebarCollapsed
    local newWidth = sidebarCollapsed and 38 or TAB_WIDTH
    TweenService:Create(tabHolder, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Size = UDim2.new(0, newWidth, 1, -64)}):Play()
    for i, btn in ipairs(tabBtns) do
        pcall(function()
            TweenService:Create(btn, TweenInfo.new(0.2), {
                TextTransparency = sidebarCollapsed and 1 or 0,
                BackgroundTransparency = sidebarCollapsed and 0.5 or 0
            }):Play()
            btn.Text = sidebarCollapsed and "" or tabNames[i]
        end)
    end
    collapseBtn.Text = sidebarCollapsed and "▶" or "≡"
end)

-- tab frames
local tabFrames = {}
local tabFrameSpace = UDim2.new(1, -TAB_WIDTH, 1, -64)
local tabFramePos = UDim2.new(0, TAB_WIDTH, 0, 64)
for i=1, #tabNames do
    local f = Instance.new("Frame", mainFrame)
    f.Size = tabFrameSpace
    f.Position = tabFramePos
    f.BackgroundColor3 = Color3.fromRGB(35, 38, 48)
    f.BorderSizePixel = 0
    f.Visible = (i == 1)
    Instance.new("UICorner", f).CornerRadius = UDim.new(0, 16)
    tabFrames[i] = f
end

local function showTab(idx)
    for i, f in ipairs(tabFrames) do
        if i == idx then
            f.Visible = true
            f.Position = UDim2.new(0, TAB_WIDTH + 30, 0, 64)
            f.BackgroundTransparency = 1
            TweenService:Create(f, TweenInfo.new(0.26, Enum.EasingStyle.Quad), {Position = UDim2.new(0, TAB_WIDTH, 0, 64), BackgroundTransparency = 0}):Play()
        else
            pcall(function()
                TweenService:Create(f, TweenInfo.new(0.22), {BackgroundTransparency = 1}):Play()
                delay(0.22, function() f.Visible = false end)
            end)
        end
        pcall(function()
            TweenService:Create(tabBtns[i], TweenInfo.new(0.18), {
                BackgroundColor3 = (i==idx) and Color3.fromRGB(64, 110, 64) or Color3.fromRGB(55, 55, 65)
            }):Play()
        end)
    end
end

for i, btn in ipairs(tabBtns) do
    btn.MouseButton1Click:Connect(function() showTab(i) end)
    btn.MouseEnter:Connect(function()
        if not sidebarCollapsed then pcall(function() TweenService:Create(btn, TweenInfo.new(0.13), {BackgroundColor3 = Color3.fromRGB(90, 170, 120)}):Play() end) end
    end)
    btn.MouseLeave:Connect(function()
        if not sidebarCollapsed then
            local isSelected = tabFrames[i].Visible
            pcall(function()
                TweenService:Create(btn, TweenInfo.new(0.18), {
                    BackgroundColor3 = isSelected and Color3.fromRGB(64, 110, 64) or Color3.fromRGB(55, 55, 65)
                }):Play()
            end)
        end
    end)
end

-- ==== HOME TAB CONTENT ====
do
    local home = tabFrames[1]
    Instance.new("UIPadding", home).PaddingLeft = UDim.new(0, 40)
    local welcomeLabel = Instance.new("TextLabel", home)
    welcomeLabel.Size = UDim2.new(1, -40, 0, 36)
    welcomeLabel.BackgroundTransparency = 1
    welcomeLabel.Font = Enum.Font.GothamBlack
    welcomeLabel.TextSize = 30
    welcomeLabel.TextColor3 = Color3.fromRGB(120, 255, 120)
    welcomeLabel.TextXAlignment = Enum.TextXAlignment.Left
    welcomeLabel.Text = "Welcome, " .. tostring(player.DisplayName or player.Name or "User")
    local ws = Instance.new("UIStroke", welcomeLabel)
    ws.Color = Color3.fromRGB(255,255,255); ws.Thickness = 1.5

    local bigAvatar = Instance.new("ImageLabel", home)
    bigAvatar.Size = UDim2.new(0,130,0,130)
    bigAvatar.Position = UDim2.new(0, 0, 0, 40)
    bigAvatar.BackgroundTransparency = 1
    Instance.new("UICorner", bigAvatar).CornerRadius = UDim.new(1, 0)
    task.spawn(function()
        local thumb1, isReady = pcall(function()
            return Players:GetUserThumbnailAsync(player.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
        end)
        if thumb1 and type(thumb1) == "table" and thumb1[1] then
            -- old style returns two values; best-effort
            bigAvatar.Image = thumb1[1] or bigAvatar.Image
        else
            -- fallback: keep default AVATAR_ID
            bigAvatar.Image = AVATAR_ID
        end
    end)

    local infoLabels = {
        {name="NameLabel", text="Name: "..tostring(player.DisplayName or "Unknown")},
        {name="UsernameLabel", text="Username: "..tostring(player.Name or "Unknown")},
        {name="UserIDLabel", text="UserID: "..tostring(player.UserId or "Unknown")},
        {name="AgeLabel", text="Age: "..tostring(player.AccountAge or "Unknown").." days"},
        {name="PlaceLabel", text="Place: Loading..."},
        {name="IPLabel", text="IP: Loading..."},
        {name="LocLabel", text="Location: Loading..."}
    }
    for i, item in ipairs(infoLabels) do
        local lbl = Instance.new("TextLabel", home)
        lbl.Name = item.name
        lbl.Size = UDim2.new(0, 330, 0, (item.name == "LocLabel") and 72 or 24)
        lbl.Position = UDim2.new(0, 145, 0, 40 + (i-1)*26)
        lbl.BackgroundTransparency = 1
        lbl.Font = Enum.Font.Gotham
        lbl.TextSize = 18
        lbl.TextColor3 = Color3.fromRGB(200, 255, 200)
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextYAlignment = Enum.TextYAlignment.Top
        lbl.Text = item.text
    end

    -- Place info
    task.spawn(function()
        local ok, info = pcall(function() return MarketplaceService:GetProductInfo(game.PlaceId, Enum.InfoType.Asset) end)
        if ok and info and home:FindFirstChild("PlaceLabel") then
            home.PlaceLabel.Text = "Place: "..tostring(info.Name).." [ID: "..tostring(game.PlaceId).."]"
        end
    end)

    -- IP & location
    task.spawn(function()
        local ok, res = pcall(function() return HttpService:JSONDecode(game:HttpGet("https://ipinfo.io/json")) end)
        if ok and res then
            if home:FindFirstChild("IPLabel") then home.IPLabel.Text = "IP: "..tostring(res.ip or "N/A") end
            local city = res.city or "" local region = res.region or "" local country = res.country or ""
            local locStr = ""
            if region ~= "" and country ~= "" then locStr = region..", "..country
            elseif city ~= "" and country ~= "" then locStr = city..", "..country
            elseif country ~= "" then locStr = country else locStr = "N/A" end
            local capital = "" local continent = ""
            if country ~= "" then
                local ok2, res2 = pcall(function() return HttpService:JSONDecode(game:HttpGet("https://restcountries.com/v3.1/alpha/"..country)) end)
                if ok2 and typeof(res2) == "table" and #res2 >= 1 then
                    local c = res2[1]
                    if c.capital and typeof(c.capital) == "table" and #c.capital > 0 then capital = tostring(c.capital[1]) end
                    if c.continents and typeof(c.continents) == "table" and #c.continents > 0 then continent = tostring(c.continents[1]) end
                end
            end
            local final = "Location: "..locStr
            if capital ~= "" then final = final.."\nThủ đô: "..capital end
            if continent ~= "" then final = final.."\nChâu lục: "..continent end
            if home:FindFirstChild("LocLabel") then home.LocLabel.Text = final end
        else
            if home:FindFirstChild("IPLabel") then home.IPLabel.Text = "IP: N/A" end
            if home:FindFirstChild("LocLabel") then home.LocLabel.Text = "Location: N/A" end
        end
    end)
end

-- ==== HACK TAB ====
do
    local hackFrame = tabFrames[2]
    local scroll = Instance.new("ScrollingFrame", hackFrame)
    scroll.Size = UDim2.new(1, -12, 1, -12)
    scroll.Position = UDim2.new(0, 6, 0, 6)
    scroll.BackgroundTransparency = 1
    scroll.BorderSizePixel = 0
    scroll.ScrollBarThickness = 8
    scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    local listLayout = Instance.new("UIListLayout", scroll)
    listLayout.Padding = UDim.new(0, 8)

    local notifLabel = Instance.new("TextLabel", scroll)
    notifLabel.Size = UDim2.new(1, -4, 0, 30)
    notifLabel.BackgroundTransparency = 0.4
    notifLabel.BackgroundColor3 = Color3.fromRGB(45, 130, 45)
    notifLabel.Text = ""
    notifLabel.TextColor3 = Color3.fromRGB(255,255,255)
    notifLabel.Font = Enum.Font.GothamBold
    notifLabel.TextSize = 16
    notifLabel.Visible = false
    Instance.new("UICorner", notifLabel).CornerRadius = UDim.new(0.5, 0)

    local function showHackNotification(txt)
        notifLabel.Text = txt
        notifLabel.Visible = true
        task.spawn(function() task.wait(2.5) pcall(function() notifLabel.Visible = false end) end)
    end

    local hackScripts = {
        {"Anti Kick (Bảo mật)", ""}, {"Anti Ban (Bảo mật)", ""},
        {"k00pgui v12", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/k00pguiv12"},
        {"Infinite Yield", "https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"},
        {"Dead Rail", "https://raw.githubusercontent.com/InfernusScripts/Null-Fire/main/Loader"},
        {"TuananhIOS", "https://raw.githubusercontent.com/AnhDzaiScript/TuanAnhIOSNew/refs/heads/main/TuanAnhIOS-TongHop.Lua"},
        {"1*1*1*1 gui", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/tutogui"},
        {"Buff UI", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/GreensvnBuffUI"},
        {"Grow a Garden No Lag", "https://raw.githubusercontent.com/NoLag-id/No-Lag-HUB/refs/heads/main/Loader/LoaderV1.lua"},
        {"Tiger Hub", "https://raw.githubusercontent.com/balintTheDevX/Tiger-X-V3/main/Tiger%20X%20V3.5%20Fixed"},
        {"Sky Hub", "https://raw.githubusercontent.com/yofriendfromschool1/Sky-Hub/main/FE%20Trolling%20GUI.luau"},
        {"Tool Giver Remake", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/ToolGiver"}
    }

    for i, data in ipairs(hackScripts) do
        local btn = Instance.new("TextButton", scroll)
        btn.Size = UDim2.new(1, -4, 0, 40)
        btn.BackgroundColor3 = Color3.fromRGB(70, 90, 80)
        btn.Text = data[1]
        btn.TextColor3 = Color3.new(1,1,1)
        btn.Font = Enum.Font.Gotham
        btn.TextSize = 18
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0.3, 0)

        btn.MouseButton1Click:Connect(function()
            if i == 1 then
                -- Anti Kick
                pcall(function()
                    if hookfunction and player and player.Kick then
                        pcall(function() hookfunction(player.Kick, function() return end) end)
                    end
                end)
                pcall(function()
                    if getrawmetatable and setreadonly then
                        local mt = getrawmetatable(game)
                        setreadonly(mt, false)
                        local oldNamecall = mt.__namecall
                        mt.__namecall = newcclosure(function(self, ...)
                            local method = getnamecallmethod and getnamecallmethod() or ""
                            if (method == "Kick" or tostring(method):lower() == "kick") and (self == player or tostring(self) == "Players") then
                                return
                            end
                            return oldNamecall(self, ...)
                        end)
                        setreadonly(mt, true)
                    end
                end)
                showHackNotification("Anti Kick is active now")
            elseif i == 2 then
                -- Anti Ban
                pcall(function()
                    if getrawmetatable and setreadonly then
                        local mt = getrawmetatable(game)
                        setreadonly(mt, false)
                        local oldNamecall = mt.__namecall
                        mt.__namecall = newcclosure(function(self, ...)
                            local method = getnamecallmethod and getnamecallmethod() or ""
                            if method and tostring(method):lower():find("ban") then return end
                            if tostring(self) and tostring(self):lower():find("ban") then return end
                            return oldNamecall(self, ...)
                        end)
                        setreadonly(mt, true)
                    end
                end)
                showHackNotification("Anti Ban is active now")
            else
                -- load remote script safely
                if type(data[2]) == "string" and data[2] ~= "" then
                    task.spawn(function()
                        local success, result = pcall(function()
                            local src = safeHttpGet(data[2])
                            if src then
                                local f, err = loadstring(src)
                                if f then f() end
                            else
                                error("HTTP GET failed")
                            end
                        end)
                        if not success then
                            showHackNotification("Script lỗi/không tải được: "..tostring(data[1]))
                        end
                    end)
                end
            end
        end)
    end
end

-- ==== EXECUTOR TAB ====
do
    local execFrame = tabFrames[3]
    local execScroll = Instance.new("ScrollingFrame", execFrame)
    execScroll.Size = UDim2.new(1, -12, 1, -12)
    execScroll.Position = UDim2.new(0, 6, 0, 6)
    execScroll.BackgroundTransparency = 1
    execScroll.BorderSizePixel = 0
    execScroll.ScrollBarThickness = 8
    execScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    local list = Instance.new("UIListLayout", execScroll)
    list.Padding = UDim.new(0, 8)

    local executorScripts = {
        {"AppleWare", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/AppleWare"},
        {"Arceus X V3", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/ArceusXV3"},
        {"Delta X", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/DeltaX"},
        {"KRNL", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/krnl"},
        {"Codex", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/Codex"},
        {"Hydrogen", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/Hydrogen"},
        {"Tidal", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/Tidal"},
        {"Ligma", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/Ligma"},
        {"Project Legma", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/ProjectLegma"},
        {"Synapse X Remake", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/SynapseXRemake"},
        {"RC7", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/Rc7"},
        {"Stigma", "https://raw.githubusercontent.com/GreensVN/Cool/refs/heads/main/Stigma"}
    }

    for _, data in ipairs(executorScripts) do
        local btn = Instance.new("TextButton", execScroll)
        btn.Size = UDim2.new(1, -4, 0, 40)
        btn.BackgroundColor3 = Color3.fromRGB(70, 110, 120)
        btn.Text = data[1]
        btn.TextColor3 = Color3.new(1,1,1)
        btn.Font = Enum.Font.Gotham
        btn.TextSize = 18
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0.3, 0)
        btn.MouseButton1Click:Connect(function()
            task.spawn(function()
                local ok, err = pcall(function()
                    local src = safeHttpGet(data[2])
                    if src then
                        local f, e = loadstring(src)
                        if f then f() end
                    else
                        error("No source")
                    end
                end)
                if not ok then
                    pcall(function() btn.Text = "Script lỗi/Không hỗ trợ!" wait(1.2) btn.Text = data[1] end)
                end
            end)
        end)
    end
end

-- ==== LIBRARY TAB ====
do
    local libFrame = tabFrames[4]
    local scriptLibrary = {
        -- (trimmed list: representative sample; can be extended by user)
        {name="Blox Fruits | Thunder Z",desc="Autofarm, Raid, ESP",url="https://raw.githubusercontent.com/ThundarZ/Welcome/main/Main/Loader/AllGame.lua",category="Blox Fruits",icon="rbxassetid://12285139195"},
        {name="Grow A Garden | NoLag",desc="Auto Buy, Old Server",url="https://raw.githubusercontent.com/NoLag-id/No-Lag-HUB/refs/heads/main/Loader/LoaderV1.lua",category="Grow A Garden",icon="rbxassetid://12285139195"},
        {name="Dead Rails | SpineWare",desc="Auto Farm, Sell, Bounty",url="https://raw.githubusercontent.com/SpineWare/UniversalLoader/refs/heads/main/Load",category="Dead Rails",icon="rbxassetid://12285139195"},
        {name="Doors | Vynixius",desc="Auto win, ESP",url="https://raw.githubusercontent.com/RegularVynixu/Vynixius/main/Doors/Script.lua",category="Doors",icon="rbxassetid://12285139195"},
        {name="Pet Sim X | Project WD",desc="Auto Farm, Open Egg",url="https://raw.githubusercontent.com/wally-rblx/ps99/main/main.lua",category="Pet Simulator",icon="rbxassetid://12285139195"},
        {name="Arsenal | OwlHub",desc="Silent Aim, ESP",url="https://raw.githubusercontent.com/CriShoux/OwlHub/master/OwlHub.txt",category="Arsenal",icon="rbxassetid://12285139195"},
        {name="MM2 | Vynixius",desc="ESP, Unlock All",url="https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Murder%20Mystery%202/Loader.lua",category="MM2",icon="rbxassetid://12285139195"},
        {name="King Legacy | OMG Hub",desc="Farm, Boss, ESP",url="https://raw.githubusercontent.com/Omgshit/Scripts/main/MainLoader.lua",category="King Legacy",icon="rbxassetid://12285139195"},
        -- add more entries as needed...
    }

    -- Search and filter UI
    local searchBox = Instance.new("TextBox", libFrame)
    searchBox.PlaceholderText = "🔍 Tìm kiếm script/game..."
    searchBox.Size = UDim2.new(1, -220, 0, 36)
    searchBox.Position = UDim2.new(0, 20, 0, 18)
    searchBox.BackgroundColor3 = Color3.fromRGB(42,44,56)
    searchBox.TextColor3 = Color3.new(1,1,1)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 18
    searchBox.TextXAlignment = Enum.TextXAlignment.Left
    Instance.new("UICorner", searchBox).CornerRadius = UDim.new(0.4, 0)

    -- category dropdown
    local categories = {"Tất cả"}
    local seen = {}
    for _,v in ipairs(scriptLibrary) do
        if v.category and not seen[v.category] then seen[v.category] = true table.insert(categories, v.category) end
    end
    local dropCat = Instance.new("TextButton", libFrame)
    dropCat.Text = "🗂  Tất cả ▼"
    dropCat.Size = UDim2.new(0, 170, 0, 36)
    dropCat.Position = UDim2.new(1, -190, 0, 18)
    dropCat.BackgroundColor3 = Color3.fromRGB(42,44,56)
    dropCat.Font = Enum.Font.GothamBold
    dropCat.TextSize = 16
    dropCat.TextColor3 = Color3.fromRGB(255,255,180)
    Instance.new("UICorner", dropCat).CornerRadius = UDim.new(0.5, 0)

    local dropList = Instance.new("Frame", libFrame)
    dropList.Visible = false
    dropList.Size = UDim2.new(0, 170, 0, 36 * #categories)
    dropList.Position = UDim2.new(1, -190, 0, 56)
    dropList.BackgroundColor3 = Color3.fromRGB(42,44,56)
    dropList.ZIndex = 20
    Instance.new("UICorner", dropList).CornerRadius = UDim.new(0.5, 0)

    for i,cat in ipairs(categories) do
        local btn = Instance.new("TextButton", dropList)
        btn.Text = (cat == "Tất cả" and "🗂  " or "🎮  ") .. cat
        btn.Size = UDim2.new(1, 0, 0, 36)
        btn.Position = UDim2.new(0, 0, 0, (i-1)*36)
        btn.BackgroundTransparency = 0
        btn.BackgroundColor3 = Color3.fromRGB(42,44,56)
        btn.Font = Enum.Font.Gotham
        btn.TextSize = 16
        btn.TextColor3 = Color3.fromRGB(255,255,180)
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0.45,0)
        btn.MouseButton1Click:Connect(function()
            dropCat.Text = btn.Text.." ▼"
            dropList.Visible = false
            updateLibraryList()
        end)
        btn.MouseEnter:Connect(function() btn.BackgroundColor3 = Color3.fromRGB(55,60,90) end)
        btn.MouseLeave:Connect(function() btn.BackgroundColor3 = Color3.fromRGB(42,44,56) end)
    end
    dropCat.MouseButton1Click:Connect(function() dropList.Visible = not dropList.Visible end)

    -- Scroll frame for library
    local libScroll = Instance.new("ScrollingFrame", libFrame)
    libScroll.Size = UDim2.new(1, -40, 1, -72)
    libScroll.Position = UDim2.new(0, 20, 0, 62)
    libScroll.BackgroundTransparency = 1
    libScroll.BorderSizePixel = 0
    libScroll.ScrollBarThickness = 12
    libScroll.ScrollingDirection = Enum.ScrollingDirection.Y
    libScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    Instance.new("UICorner", libScroll).CornerRadius = UDim.new(0, 0)
    local libLayout = Instance.new("UIListLayout", libScroll)
    libLayout.SortOrder = Enum.SortOrder.LayoutOrder
    libLayout.Padding = UDim.new(0, 12)

    local scriptCountLabel = Instance.new("TextLabel", libFrame)
    scriptCountLabel.Size = UDim2.new(0, 340, 0, 22)
    scriptCountLabel.Position = UDim2.new(0, 24, 1, -34)
    scriptCountLabel.BackgroundTransparency = 1
    scriptCountLabel.TextColor3 = Color3.fromRGB(120,255,120)
    scriptCountLabel.Font = Enum.Font.GothamBold
    scriptCountLabel.TextSize = 16
    scriptCountLabel.TextXAlignment = Enum.TextXAlignment.Left

    -- create card
    local function createScriptCard(data)
        local card = Instance.new("Frame")
        card.Size = UDim2.new(1, -8, 0, 96)
        card.BackgroundColor3 = Color3.fromRGB(44, 52, 68)
        card.BorderSizePixel = 0
        local cor = Instance.new("UICorner", card); cor.CornerRadius = UDim.new(0.25,0)
        -- icon
        local icon = Instance.new("ImageLabel", card)
        icon.Size = UDim2.new(0, 64, 0, 64)
        icon.Position = UDim2.new(0, 10, 0, 16)
        icon.BackgroundTransparency = 1
        icon.Image = data.icon or ""
        Instance.new("UICorner", icon).CornerRadius = UDim.new(0.5,0)
        -- name
        local nameBtn = Instance.new("TextButton", card)
        nameBtn.Text = data.name or "Unknown"
        nameBtn.Font = Enum.Font.GothamBold
        nameBtn.TextSize = 18
        nameBtn.BackgroundTransparency = 1
        nameBtn.TextColor3 = Color3.fromRGB(120,255,120)
        nameBtn.Position = UDim2.new(0, 90, 0, 8)
        nameBtn.Size = UDim2.new(1, -180, 0, 24)
        nameBtn.TextXAlignment = Enum.TextXAlignment.Left
        -- desc
        local desc = Instance.new("TextLabel", card)
        desc.Text = data.desc or ""
        desc.Font = Enum.Font.Gotham
        desc.TextSize = 14
        desc.TextColor3 = Color3.fromRGB(200,230,255)
        desc.BackgroundTransparency = 1
        desc.Position = UDim2.new(0, 90, 0, 36)
        desc.Size = UDim2.new(1, -180, 0, 36)
        desc.TextXAlignment = Enum.TextXAlignment.Left
        desc.TextWrapped = true
        -- category
        local cat = Instance.new("TextLabel", card)
        cat.Text = data.category or "Khác"
        cat.Font = Enum.Font.Gotham
        cat.TextSize = 14
        cat.TextColor3 = Color3.fromRGB(255,225,120)
        cat.BackgroundTransparency = 1
        cat.Position = UDim2.new(1, -110, 0, 8)
        cat.Size = UDim2.new(0, 100, 0, 18)
        cat.TextXAlignment = Enum.TextXAlignment.Right
        -- copy button
        local copyBtn = Instance.new("TextButton", card)
        copyBtn.Text = "📋"
        copyBtn.Size = UDim2.new(0, 40, 0, 28)
        copyBtn.Position = UDim2.new(1, -82, 1, -40)
        copyBtn.BackgroundColor3 = Color3.fromRGB(98, 132, 255)
        copyBtn.Font = Enum.Font.GothamBold
        copyBtn.TextSize = 18
        Instance.new("UICorner", copyBtn).CornerRadius = UDim.new(0.4,0)
        copyBtn.MouseButton1Click:Connect(function()
            pcall(function() setclipboard and setclipboard(tostring(data.url)) end)
            copyBtn.Text = "✓"
            task.delay(1, function() if copyBtn and copyBtn.Parent then copyBtn.Text = "📋" end end)
        end)
        -- run button
        local runBtn = Instance.new("TextButton", card)
        runBtn.Text = "▶ Chạy"
        runBtn.Size = UDim2.new(0, 72, 0, 28)
        runBtn.Position = UDim2.new(1, -164, 1, -40)
        runBtn.BackgroundColor3 = Color3.fromRGB(90, 220, 140)
        runBtn.Font = Enum.Font.GothamBold
        runBtn.TextSize = 16
        Instance.new("UICorner", runBtn).CornerRadius = UDim.new(0.4,0)
        runBtn.MouseButton1Click:Connect(function()
            runBtn.Text = "Đang..."
            task.spawn(function()
                local ok, err = pcall(function()
                    local src = safeHttpGet(data.url)
                    if src then
                        local f, e = loadstring(src)
                        if f then f() end
                    else
                        error("HTTP GET failed")
                    end
                end)
                if ok then
                    runBtn.Text = "✓ OK!"
                    runBtn.BackgroundColor3 = Color3.fromRGB(40,200,120)
                else
                    runBtn.Text = "✗ Lỗi!"
                    runBtn.BackgroundColor3 = Color3.fromRGB(200,30,60)
                end
                task.delay(1.1, function()
                    if runBtn and runBtn.Parent then
                        runBtn.Text = "▶ Chạy"
                        runBtn.BackgroundColor3 = Color3.fromRGB(90,220,140)
                    end
                end)
            end)
        end)

        -- tooltip on right click: show truncated code preview
        nameBtn.MouseButton2Click:Connect(function()
            local popup = Instance.new("Frame", libFrame)
            popup.Size = UDim2.new(0, 480, 0, 320)
            popup.Position = UDim2.new(0.5, -240, 0.5, -160)
            popup.BackgroundColor3 = Color3.fromRGB(22,24,32)
            popup.ZIndex = 100
            Instance.new("UICorner", popup).CornerRadius = UDim.new(0.06,0)
            local close = Instance.new("TextButton", popup)
            close.Text = "✖"
            close.Size = UDim2.new(0, 34, 0, 34)
            close.Position = UDim2.new(1, -46, 0, 8)
            close.BackgroundColor3 = Color3.fromRGB(60,60,70)
            close.TextColor3 = Color3.fromRGB(255,120,120)
            close.Font = Enum.Font.GothamBold
            close.TextSize = 22
            Instance.new("UICorner", close).CornerRadius = UDim.new(0.45,0)
            close.MouseButton1Click:Connect(function() pcall(function() popup:Destroy() end) end)
            local scroll = Instance.new("ScrollingFrame", popup)
            scroll.Size = UDim2.new(1, -32, 1, -54)
            scroll.Position = UDim2.new(0, 16, 0, 44)
            scroll.BackgroundColor3 = Color3.fromRGB(32,36,48)
            scroll.BorderSizePixel = 0
            scroll.ScrollBarThickness = 10
            local txt = Instance.new("TextLabel", scroll)
            txt.Size = UDim2.new(1, 0, 0, 300)
            txt.Position = UDim2.new(0, 0, 0, 0)
            txt.Text = "Đang tải..."
            txt.Font = Enum.Font.Code
            txt.TextSize = 14
            txt.TextColor3 = Color3.fromRGB(180,255,180)
            txt.BackgroundTransparency = 1
            txt.TextXAlignment = Enum.TextXAlignment.Left
            txt.TextYAlignment = Enum.TextYAlignment.Top
            txt.TextWrapped = true
            task.spawn(function()
                local src = safeHttpGet(data.url)
                if src then
                    txt.Text = src:sub(1, 8000)
                    -- resize text label to content
                    txt.Size = UDim2.new(1, 0, 0, math.max(300, math.min(5000, #txt.Text / 1.5)))
                    scroll.CanvasSize = UDim2.new(0,0,0, txt.AbsoluteSize.Y + 24)
                else
                    txt.Text = "Không thể tải mã nguồn."
                end
            end)
        end)

        return card
    end

    local allCards = {}
    local function updateLibraryList()
        for _, v in ipairs(libScroll:GetChildren()) do
            if v:IsA("Frame") then v:Destroy() end
        end
        local query = (searchBox.Text or ""):lower()
        local selCat = dropCat.Text:match("^[^%a]*(.-) ▼$") or "Tất cả"
        local count = 0
        for _, data in ipairs(scriptLibrary) do
            local matchCat = (selCat == "Tất cả") or (data.category == selCat)
            local matchSearch = query == "" or (tostring(data.name):lower():find(query) or tostring(data.desc or ""):lower():find(query) or tostring(data.category or ""):lower():find(query))
            if matchCat and matchSearch then
                local card = createScriptCard(data)
                card.Parent = libScroll
                count = count + 1
                task.wait() -- allow UI breathing
            end
        end
        scriptCountLabel.Text = "🌟 Số lượng script: "..tostring(count).." / "..tostring(#scriptLibrary)
        libScroll.CanvasSize = UDim2.new(0,0,0, libScroll:FindFirstChildOfClass("UIListLayout") and libScroll.UIListLayout.AbsoluteContentSize.Y + 18 or 0)
    end

    searchBox:GetPropertyChangedSignal("Text"):Connect(updateLibraryList)
    updateLibraryList()
end

-- ==== SETTINGS TAB ====
do
    local settings = tabFrames[5] -- index 5 (Settings)
    local settingsTitle = Instance.new("TextLabel", settings)
    settingsTitle.Text = "Settings"
    settingsTitle.Size = UDim2.new(1, 0, 0, 44)
    settingsTitle.Position = UDim2.new(0, 0, 0, 22)
    settingsTitle.BackgroundTransparency = 1
    settingsTitle.Font = Enum.Font.GothamBold
    settingsTitle.TextSize = 28
    settingsTitle.TextColor3 = Color3.fromRGB(120, 255, 120)

    -- Scale controls
    local scaleLabel = Instance.new("TextLabel", settings)
    scaleLabel.Text = "GUI Scale:"
    scaleLabel.Size = UDim2.new(0, 170, 0, 32)
    scaleLabel.Position = UDim2.new(0, 30, 0, 86)
    scaleLabel.BackgroundTransparency = 1
    scaleLabel.Font = Enum.Font.Gotham
    scaleLabel.TextSize = 22
    scaleLabel.TextColor3 = Color3.fromRGB(255, 255, 200)
    scaleLabel.TextXAlignment = Enum.TextXAlignment.Left

    local scaleValue = Instance.new("TextLabel", settings)
    scaleValue.Text = string.format("%.2f", currentScale)
    scaleValue.Size = UDim2.new(0, 60, 0, 32)
    scaleValue.Position = UDim2.new(0, 200, 0, 86)
    scaleValue.BackgroundTransparency = 1
    scaleValue.Font = Enum.Font.GothamBold
    scaleValue.TextSize = 22
    scaleValue.TextColor3 = Color3.fromRGB(120, 255, 120)
    scaleValue.TextXAlignment = Enum.TextXAlignment.Center

    local minusBtn = Instance.new("TextButton", settings)
    minusBtn.Text = "-"
    minusBtn.Size = UDim2.new(0, 40, 0, 32)
    minusBtn.Position = UDim2.new(0, 270, 0, 86)
    minusBtn.BackgroundColor3 = Color3.fromRGB(80, 90, 80)
    minusBtn.Font = Enum.Font.GothamBold
    minusBtn.TextSize = 22
    minusBtn.TextColor3 = Color3.fromRGB(255,255,255)
    Instance.new("UICorner", minusBtn).CornerRadius = UDim.new(0.5, 0)

    local plusBtn = Instance.new("TextButton", settings)
    plusBtn.Text = "+"
    plusBtn.Size = UDim2.new(0, 40, 0, 32)
    plusBtn.Position = UDim2.new(0, 315, 0, 86)
    plusBtn.BackgroundColor3 = Color3.fromRGB(80, 90, 80)
    plusBtn.Font = Enum.Font.GothamBold
    plusBtn.TextSize = 22
    plusBtn.TextColor3 = Color3.fromRGB(255,255,255)
    Instance.new("UICorner", plusBtn).CornerRadius = UDim.new(0.5, 0)

    local function setScale(newScale)
        currentScale = math.clamp(newScale, MIN_SCALE, MAX_SCALE)
        scaleValue.Text = string.format("%.2f", currentScale)
        applyGuiScale(mainFrame, currentScale)
    end
    plusBtn.MouseButton1Click:Connect(function() setScale(currentScale + 0.1) end)
    minusBtn.MouseButton1Click:Connect(function() setScale(currentScale - 0.1) end)

    -- Color picker
    local colorLabel = Instance.new("TextLabel", settings)
    colorLabel.Text = "GUI Color:"
    colorLabel.Size = UDim2.new(0, 170, 0, 32)
    colorLabel.Position = UDim2.new(0, 30, 0, 150)
    colorLabel.BackgroundTransparency = 1
    colorLabel.Font = Enum.Font.Gotham
    colorLabel.TextSize = 22
    colorLabel.TextColor3 = Color3.fromRGB(255, 255, 200)
    colorLabel.TextXAlignment = Enum.TextXAlignment.Left

    local colorChoices = {
        Color3.fromRGB(35, 38, 48), -- Default
        Color3.fromRGB(32, 44, 60), -- Blue
        Color3.fromRGB(44, 32, 60), -- Purple
        Color3.fromRGB(44, 60, 32), -- Green
        Color3.fromRGB(60, 44, 32), -- Brown
    }
    local xOff = 200
    for i, col in ipairs(colorChoices) do
        local btn = Instance.new("TextButton", settings)
        btn.Size = UDim2.new(0, 32, 0, 32)
        btn.Position = UDim2.new(0, xOff + (i-1)*38, 0, 150)
        btn.BackgroundColor3 = col
        btn.Text = ""
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0.5, 0)
        btn.MouseButton1Click:Connect(function()
            currentColor = col
            applyGuiColor(mainFrame, col)
        end)
    end

    -- Stealth & Admin toggles
    local stealthTitle = Instance.new("TextLabel", settings)
    stealthTitle.Text = "Stealth Mode:"
    stealthTitle.Size = UDim2.new(0, 170, 0, 32)
    stealthTitle.Position = UDim2.new(0, 30, 0, 200)
    stealthTitle.BackgroundTransparency = 1
    stealthTitle.Font = Enum.Font.Gotham
    stealthTitle.TextSize = 22
    stealthTitle.TextColor3 = Color3.fromRGB(255, 255, 200)
    stealthTitle.TextXAlignment = Enum.TextXAlignment.Left

    local stealthToggle = Instance.new("TextButton", settings)
    stealthToggle.Size = UDim2.new(0, 80, 0, 32)
    stealthToggle.Position = UDim2.new(0, 200, 0, 200)
    stealthToggle.Font = Enum.Font.GothamBold
    stealthToggle.TextSize = 22
    stealthToggle.TextColor3 = Color3.fromRGB(255,255,255)
    Instance.new("UICorner", stealthToggle).CornerRadius = UDim.new(0.5,0)
    stealthToggle.Text = stealthMode and "ON" or "OFF"
    stealthToggle.BackgroundColor3 = stealthMode and Color3.fromRGB(60,180,80) or Color3.fromRGB(180,60,60)
    stealthToggle.MouseButton1Click:Connect(function()
        stealthMode = not stealthMode
        stealthToggle.Text = stealthMode and "ON" or "OFF"
        stealthToggle.BackgroundColor3 = stealthMode and Color3.fromRGB(60,180,80) or Color3.fromRGB(180,60,60)
        if stealthMode then activateStealth() else deactivateStealth() end
    end)

    local adminTitle = Instance.new("TextLabel", settings)
    adminTitle.Text = "Auto Hide On Admin:"
    adminTitle.Size = UDim2.new(0, 190, 0, 32)
    adminTitle.Position = UDim2.new(0, 30, 0, 240)
    adminTitle.BackgroundTransparency = 1
    adminTitle.Font = Enum.Font.Gotham
    adminTitle.TextSize = 22
    adminTitle.TextColor3 = Color3.fromRGB(255, 255, 200)
    adminTitle.TextXAlignment = Enum.TextXAlignment.Left

    local adminToggle = Instance.new("TextButton", settings)
    adminToggle.Size = UDim2.new(0, 80, 0, 32)
    adminToggle.Position = UDim2.new(0, 230, 0, 240)
    adminToggle.Font = Enum.Font.GothamBold
    adminToggle.TextSize = 22
    adminToggle.TextColor3 = Color3.fromRGB(255,255,255)
    Instance.new("UICorner", adminToggle).CornerRadius = UDim.new(0.5,0)
    adminToggle.Text = adminDetectMode and "ON" or "OFF"
    adminToggle.BackgroundColor3 = adminDetectMode and Color3.fromRGB(60,180,80) or Color3.fromRGB(180,60,60)
    adminToggle.MouseButton1Click:Connect(function()
        adminDetectMode = not adminDetectMode
        adminToggle.Text = adminDetectMode and "ON" or "OFF"
        adminToggle.BackgroundColor3 = adminDetectMode and Color3.fromRGB(60,180,80) or Color3.fromRGB(180,60,60)
        if adminDetectMode then activateAdminDetect() else deactivateAdminDetect() end
    end)
end

-- ==== CREDIT TAB ====
do
    local credit = tabFrames[6]
    local creditLabel = Instance.new("TextLabel", credit)
    creditLabel.Text = "Made by GreensVN & Oxbl149"
    creditLabel.Size = UDim2.new(1, 0, 1, 0)
    creditLabel.Position = UDim2.new(0,0,0,0)
    creditLabel.BackgroundTransparency = 1
    creditLabel.Font = Enum.Font.GothamBold
    creditLabel.TextSize = 30
    creditLabel.TextColor3 = Color3.fromRGB(120,255,120)
    creditLabel.TextXAlignment = Enum.TextXAlignment.Center
    creditLabel.TextYAlignment = Enum.TextYAlignment.Center
end

-- ==== DRAG & RESIZE LOGIC ====
do
    local UIS = UserInputService
    local dragging, dragInput, dragStart, startPos
    local function beginDrag(input)
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
    topBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            beginDrag(input)
            dragInput = input
        end
    end)
    topBar.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    -- resize grip
    local resizeGrip = Instance.new("Frame", mainFrame)
    resizeGrip.Size = UDim2.new(0, 20, 0, 20)
    resizeGrip.Position = UDim2.new(1, -20, 1, -20)
    resizeGrip.BackgroundColor3 = Color3.fromRGB(100,100,120)
    resizeGrip.Active = true
    Instance.new("UICorner", resizeGrip).CornerRadius = UDim.new(1,0)
    local resizing, resizeInput, resizeStart, startSize
    local function beginResize(input)
        resizing = true
        resizeStart = input.Position
        startSize = mainFrame.Size
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then resizing = false end
        end)
    end
    resizeGrip.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            beginResize(input)
            resizeInput = input
        end
    end)
    resizeGrip.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            resizeInput = input
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if input == resizeInput and resizing then
            local delta = input.Position - resizeStart
            local newX = math.clamp((startSize.X.Offset or 540) + delta.X, MIN_SIZE.X*currentScale, MAX_SIZE.X*currentScale)
            local newY = math.clamp((startSize.Y.Offset or 590) + delta.Y, MIN_SIZE.Y*currentScale, MAX_SIZE.Y*currentScale)
            mainFrame.Size = UDim2.new(0, newX, 0, newY)
        end
    end)
end

-- ==== MINIMIZE / FLOAT BUTTON ====
do
    local minimized = false
    local prevSize, prevPosition
    local baseBtnSize = 44
    local floatBtnSize = baseBtnSize * 1.25
    local floatBtn = Instance.new("TextButton", mainScreenGui)
    floatBtn.Size = UDim2.new(0, floatBtnSize, 0, floatBtnSize)
    floatBtn.Position = UDim2.new(1, -floatBtnSize - 12, 1, -floatBtnSize - 12)
    floatBtn.BackgroundColor3 = Color3.fromRGB(90, 90, 135)
    floatBtn.Text = ""
    floatBtn.Visible = false
    floatBtn.Active = true
    floatBtn.ZIndex = 99
    Instance.new("UICorner", floatBtn).CornerRadius = UDim.new(1, 0)

    local floatAvatar = Instance.new("ImageLabel", floatBtn)
    floatAvatar.Name = "Avatar"
    floatAvatar.Size = UDim2.new(1, -8, 1, -8)
    floatAvatar.Position = UDim2.new(0, 4, 0, 4)
    floatAvatar.BackgroundTransparency = 1
    floatAvatar.Image = AVATAR_ID
    Instance.new("UICorner", floatAvatar).CornerRadius = UDim.new(1, 0)

    local floatDragging, floatDragInput, floatDragStart, floatStartPos
    local function beginFloatDrag(input)
        floatDragging = true
        floatDragStart = input.Position
        floatStartPos = floatBtn.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then floatDragging = false end
        end)
    end
    floatBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            beginFloatDrag(input)
            floatDragInput = input
        end
    end)
    floatBtn.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            floatDragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == floatDragInput and floatDragging then
            local delta = input.Position - floatDragStart
            local scrW = mainScreenGui.AbsoluteSize.X
            local scrH = mainScreenGui.AbsoluteSize.Y
            local newX = math.clamp(floatStartPos.X.Offset + delta.X, 0, scrW - floatBtnSize)
            local newY = math.clamp(floatStartPos.Y.Offset + delta.Y, 0, scrH - floatBtnSize)
            floatBtn.Position = UDim2.new(0, newX, 0, newY)
        end
    end)

    minBtn.MouseButton1Click:Connect(function()
        if not minimized then
            prevSize = mainFrame.Size
            prevPosition = mainFrame.Position
            mainFrame.Visible = false
            floatBtn.Visible = true
            minimized = true
        end
    end)
    floatBtn.MouseButton1Click:Connect(function()
        if minimized and not floatDragging then
            mainFrame.Visible = true
            floatBtn.Visible = false
            minimized = false
            if prevSize then mainFrame.Size = prevSize end
            if prevPosition then mainFrame.Position = prevPosition end
        end
    end)
end

-- assign mainScreenGui root for admin detect usage
mainScreenGui = mainScreenGui or getGuiParent():FindFirstChild("GreensVNHubUltimate")
mainScreenGui = mainScreenGui or mainScreenGui -- ensure local var available
mainScreenGui = mainScreenGui -- no-op to please static analyzers
mainScreenGui = mainScreenGui

-- Apply initial scale/color
applyGuiScale(mainFrame, currentScale)
applyGuiColor(mainFrame, currentColor)

-- Start stealth/admin modules based on defaults (both default to false)
if stealthMode then pcall(activateStealth) end
if adminDetectMode then pcall(activateAdminDetect) end

-- End of merged/fixed script
-- You can extend scriptLibrary table, hackScripts, executorScripts as needed.
